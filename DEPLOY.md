# 🚀 Інструкції для деплою на Vercel

## Швидкий старт

### Варіант 1: Через Vercel Dashboard (Рекомендовано)

1. **Зайдіть на [vercel.com](https://vercel.com)** та увійдіть через GitHub/GitLab/Bitbucket

2. **Натисніть "Add New Project"**

3. **Імпортуйте ваш репозиторій:**
   - Оберіть репозиторій зі списку
   - Vercel автоматично визначить налаштування

4. **Перевірте конфігурацію:**
   - Framework Preset: **Other**
   - Root Directory: **`./`** (корінь)
   - Build Command: **(залиште порожнім)**
   - Output Directory: **`./`** (корінь)
   - Install Command: **(залиште порожнім)**

5. **Натисніть "Deploy"** 🚀

### Варіант 2: Через Vercel CLI

1. **Встановіть Vercel CLI:**
   ```bash
   npm i -g vercel
   ```

2. **Увійдіть в Vercel:**
   ```bash
   vercel login
   ```

3. **Деплой:**
   ```bash
   vercel
   ```

4. **Для production:**
   ```bash
   vercel --prod
   ```

## 📋 Конфігурація (вже налаштована)

Файл `vercel.json` містить:

- ✅ **Rewrites** для чистих URL:
  - `/istoriya` → `/istoriya.html`
  - `/akademiya` → `/akademiya.html`
  - `/pidtrymka` → `/pidtrymka.html`
  - `/museum` → `/museum.html`

- ✅ **Headers для кешування:**
  - Статичні ресурси (CSS, JS): 1 рік
  - Зображення/відео: 1 рік
  - HTML: 1 година

## 🔧 Налаштування Environment Variables (за потреби)

Якщо потрібні змінні оточення:

1. Перейдіть в **Project Settings** → **Environment Variables**
2. Додайте змінні:
   - `NODE_ENV=production`
   - Інші за потреби

## ✅ Перевірка після деплою

1. **Перевірте чи відкривається сайт:**
   - Відкрийте URL з Vercel (наприклад, `your-project.vercel.app`)

2. **Перевірте чи працюють rewrites:**
   - `/istoriya` має відкрити `istoriya.html`
   - `/akademiya` має відкрити `akademiya.html`

3. **Перевірте перемикач мов:**
   - Натисніть UA/EN/IT
   - Переконайтеся, що контент перекладається

4. **Перевірте анімації:**
   - Прокрутіть сторінку
   - Переконайтеся, що GSAP анімації працюють

## 🐛 Розв'язання проблем

### Проблема: 404 на чистих URL

**Рішення:** Перевірте, чи файл `vercel.json` знаходиться в корені проекту.

### Проблема: Стилі не завантажуються

**Рішення:** Перевірте шляхи в HTML - вони мають бути відносними (`style.css`, не `/style.css`)

### Проблема: Зображення не показуються

**Рішення:** Переконайтеся, що папка `images/` закомічена в git.

## 📊 Performance Tips

1. **Включіть Automatic HTTPS** (Vercel робить це автоматично)

2. **Використовуйте Vercel Analytics** (опціонально):
   - Project Settings → Analytics → Enable

3. **Налаштуйте Custom Domain** (якщо є):
   - Project Settings → Domains → Add Domain

## 🔄 Continuous Deployment

Vercel автоматично деплоїть нові коміти:
- Push в `main` → Production деплой
- Push в інші гілки → Preview деплой

## 📝 Чек-лист перед деплоєм

- [ ] Всі файли закомічені в git
- [ ] `vercel.json` створений та коректний
- [ ] Всі шляхи до ресурсів перевірені
- [ ] Тестовано локально
- [ ] README.md оновлений

## 🎉 Готово!

Після успішного деплою ваш сайт буде доступний за адресою:
- Production: `your-project.vercel.app`
- Custom domain (якщо налаштовано): `your-domain.com`

---

**Підтримка:** [Vercel Documentation](https://vercel.com/docs)

